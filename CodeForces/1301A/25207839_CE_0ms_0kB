#include <bits/stdc++.h>

using namespace std;

int main()
{
    string a, b, c;

    int t;

    cin >> t;

    while(t--)
    {
        cin >> a >> b >> c;

        int r = 1, i;

        for(i = 0 ; i < (int)a.size(); i++)
        {
            if(c[i] != a[i] && c[i] != b[i])
            {
                r = 0;
            }
        }
        if(r==1)
            puts("YES");
        else
            puts("NO");

        return 0;
    }
}
